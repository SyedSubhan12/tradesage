# FIXED: Updated requirements.txt with Pydantic v2 compatibility and version constraints

# Core FastAPI and web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0

# FIXED: Updated Pydantic to v2 with settings support
pydantic==2.5.0
pydantic-settings==2.1.0

# Database and async support
asyncpg==0.29.0
sqlalchemy==2.0.23
alembic==1.13.1

# Redis and caching
redis==5.0.1

# Data processing
pandas==2.1.4
numpy==1.24.4

# FIXED: Added databento client (if available via pip, otherwise install via git)
# databento==0.18.0  # Uncomment if available on PyPI
# If not available on PyPI, install via: pip install git+https://github.com/databento-inc/databento-python.git

# Monitoring and observability
prometheus-client==0.19.0
structlog==23.2.0

# OpenTelemetry (optional - comment out if not using distributed tracing)
opentelemetry-api==1.21.0
opentelemetry-sdk==1.21.0
opentelemetry-exporter-jaeger==1.21.0

# HTTP client
httpx==0.25.2

# Additional async and performance libraries
asyncio==3.4.3
uvloop==0.19.0  # High-performance event loop
httptools==0.6.1  # High-performance HTTP parser

# Data validation and utilities
python-multipart==0.0.6  # For form data handling
python-jose[cryptography]==3.3.0  # For JWT tokens
passlib[bcrypt]==1.7.4  # For password hashing

# Development and testing (optional - can be moved to dev-requirements.txt)
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-mock==3.12.0
httpx==0.25.2  # For testing HTTP clients

# System monitoring (optional)
psutil==5.9.6  # For system monitoring in health checks

# Data compression (optional but recommended for performance)
lz4==4.3.2  # Fast compression
brotli==1.1.0  # Web compression

# Time and date utilities
python-dateutil==2.8.2
pytz==2023.3

# Security (production recommended)
cryptography==41.0.8

# Configuration and environment
python-dotenv==1.0.0  # For .env file support

# Logging enhancements
colorlog==6.8.0  # Colored logging for development

# WebSocket support (already included in FastAPI but explicit for clarity)
websockets==12.0

# Optional: Machine learning (uncomment if using ML features)
# scikit-learn==1.3.2
# tensorflow==2.15.0

# Optional: Financial/Trading libraries (uncomment if needed)
# TA-Lib==0.4.28  # Technical analysis
# yfinance==0.2.28  # Yahoo Finance API
# ccxt==4.1.77  # Cryptocurrency exchange APIs

# Development tools (optional - move to dev-requirements.txt)
black==23.12.0  # Code formatting
isort==5.13.2  # Import sorting
flake8==6.1.0  # Linting
mypy==1.8.0  # Type checking

# Docker and containerization support
gunicorn==21.2.0  # WSGI server for production deployment